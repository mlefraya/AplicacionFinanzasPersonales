<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme using Material3 -->
    <style name="Base.Theme.FinanzasPersonalesNuevo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores y atributos base -->
        <item name="colorPrimary">@color/blue_700</item>
        <item name="colorPrimaryContainer">@color/blue_900</item>
        <item name="colorSecondary">@color/amber_500</item>
        <item name="colorSecondaryContainer">@color/amber_700</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/gray</item>
        <!-- Configurar el drawable del cursor -->
        <item name="android:textCursorDrawable">@drawable/cursor_blue</item>
        <!-- Puedes agregar aquí otros atributos personalizados -->
        <item name="android:colorControlActivated">@color/blue_700</item>

    </style>

    <!-- Tema de la aplicación -->
    <style name="Theme.FinanzasPersonalesNuevo" parent="Base.Theme.FinanzasPersonalesNuevo">
        <!-- Puedes sobreescribir o agregar más atributos específicos del tema aquí -->
    </style>

    <!-- Estilo para la Toolbar -->
    <style name="Widget.FinanzasPersonal.Toolbar" parent="Widget.Material3.Toolbar">
        <item name="android:background">@color/blue_700</item>
        <item name="titleTextColor">@color/white</item>
    </style>

    <!-- Estilo para botones -->
    <style name="Widget.FinanzasPersonal.Button" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/blue_700</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
